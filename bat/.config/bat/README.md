# Theme Configuration

Bat themes are listed in the `themes` directory.
And to change Bat theme, you can go to `~/.zshrc` and update the `BAT_THEME` variable.

```sh
export BAT_THEME='Catppuccin Mocha'
```
